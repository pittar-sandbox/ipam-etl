# Camel Config
camel.context.name=ipam-etl
camel.main.routes-include-pattern=classpath:routes/*.yaml

# MongoDB Config (Placeholder - adjust as needed)
# quarkus.mongodb.connection-string=mongodb://localhost:27017
quarkus.mongodb.connection-string=mongodb://user:password@localhost:27017
quarkus.mongodb.database=ipam
quarkus.mongodb.devservices.enabled=false

# IPAM Output Configuration
ipam.output.endpoint=mongodb:camelMongoClient?database=ipam&collection=ipam_records&operation=insert

# Logging
quarkus.log.category."ca.pitt.demo".level=INFO
quarkus.log.category."org.apache.camel".level=INFO
# LLM Client Configuration
# llm-api/mp-rest/url=https://rhaiis-inference-demo.apps.prime.pitt.ca
# llm-api/mp-rest/connectTimeout=5000
# llm-api/mp-rest/readTimeout=120000

## Alternative Configuration using configKey "llm-api" and milliseconds
quarkus.rest-client."llm-api".url=https://<your llm endpoint here>
quarkus.rest-client."llm-api".connect-timeout=240000
quarkus.rest-client."llm-api".read-timeout=240000

# 1. Enable logging for request and response traffic
quarkus.rest-client.logging.scope=request-response

# 2. Set the body limit high enough to capture the full error message
# 5000 bytes (5KB) should be enough for an HTML error page
quarkus.rest-client.logging.body-limit=5000

# 3. Set the logging category level to DEBUG
quarkus.log.category."org.jboss.resteasy.reactive.client.logging".level=DEBUG